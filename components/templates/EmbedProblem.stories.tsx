import EmbedProblem from "./EmbedProblem";

export default {
  title: "templates/EmbedProblem",
  component: EmbedProblem,
};

const Template = () => <EmbedProblem />;

export const Default = Template.bind({});
