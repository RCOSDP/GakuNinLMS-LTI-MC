import UserSettingsContainer from "$templates/UserSettingsContainer";

function UserSettings() {
  return <UserSettingsContainer />;
}

function Router() {
  return <UserSettings />;
}

export default Router;
