import makeStyles from "@mui/styles/makeStyles";

const accordionDetail = makeStyles({
  root: {
    display: "block",
    paddingRight: 0,
    paddingTop: 0,
    paddingLeft: 0,
  },
});

export default accordionDetail;
