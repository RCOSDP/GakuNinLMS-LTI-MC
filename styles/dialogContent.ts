import makeStyles from "@mui/styles/makeStyles";
import gray from "theme/colors/gray";

// TODO: makeStylesからstyledに移行したい

const dialogContent = makeStyles({
  root: {
    backgroundColor: gray[50],
  },
});

export default dialogContent;
